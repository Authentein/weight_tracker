<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, maximum-scale=1.0, user-scalable=no" />
    <title>Deehan Master Schedule</title>
    <meta name="description" content="Integrated Master Plan — June–December 2026" />

    <!-- iOS PWA: makes "Add to Home Screen" open full-screen like a native app -->
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <meta name="apple-mobile-web-app-title" content="Master Plan" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="theme-color" content="#0b0d12" />

    <link rel="manifest" href="./manifest.webmanifest" />
    <link rel="apple-touch-icon" href="./icon-180.png" />

    <style>
      html, body, #root { margin: 0; padding: 0; background: #0b0d12; min-height: 100vh; }
      /* Respect iPhone safe areas (notch / home indicator) */
      body { padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left); box-sizing: border-box; }
      * { -webkit-tap-highlight-color: transparent; }
    </style>
    <script type="module" crossorigin src="./assets/index-ILxPdU3A.js"></script>
  </head>
  <body>
    <div id="root"></div>
  </body>
</html>
